<div
  data-action="keydown.escape->modal#close"
  data-modal-target="overlay"
  class="fixed inset-0 z-50 items-center justify-center bg-black bg-opacity-50 hidden"
>
  <div data-modal-target="content" class="bg-white rounded-xl shadow-xl p-6 max-w-lg w-full relative">
    <button
      type="button"
      class="absolute top-2 right-2 text-gray-400 hover:text-gray-600"
      data-action="click->modal#close"
    >
      ✕
    </button>

    <%= yield %>
  </div>
</div>